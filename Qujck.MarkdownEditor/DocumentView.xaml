<UserControl
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:avalonEdit="http://icsharpcode.net/sharpdevelop/avalonedit"
    xmlns:local="clr-namespace:Qujck.MarkdownEditor"
    xmlns:behaviours="clr-namespace:Qujck.MarkdownEditor.Behaviours"
    xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
    xmlns:ei="http://schemas.microsoft.com/expression/2010/interactions"
    x:Class="Qujck.MarkdownEditor.DocumentView"
    mc:Ignorable="d" 
    d:DesignHeight="300" d:DesignWidth="300">
    <i:Interaction.Behaviors>
        <behaviours:DocumentViewInitialiseAvalonEdit StringResourceProvider="{local:DependencyResolver}"/>
        <behaviours:DocumentViewInitialiseRenderedView HtmlQueryHandler="{local:DependencyResolver}"/>
        <behaviours:DocumentViewScrollBehaviour/>
        <behaviours:DocumentViewRefreshBehaviour WriteDocumentCommandHandler="{local:DependencyResolver}"/>
    </i:Interaction.Behaviors>
    <Grid x:Name="MainGrid" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <avalonEdit:TextEditor
			x:Name="TextEditor"
			Grid.Column="0"
			FontFamily="Segoe UI"
			FontSize="12pt"
			ScrollViewer.CanContentScroll="True" 
			ScrollViewer.HorizontalScrollBarVisibility="Auto" 
			ScrollViewer.VerticalScrollBarVisibility="Auto"
			WordWrap="True" 
			BorderThickness="0" 
			Margin="0"
			Padding="25"/>
        <WebBrowser 
			x:Name="RenderedView" 
			Grid.Column="1"
			ScrollViewer.VerticalScrollBarVisibility="Auto"
			ScrollViewer.HorizontalScrollBarVisibility="Disabled"/>
    </Grid>
</UserControl>
